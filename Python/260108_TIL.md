# 2026-01-08

### problem8 문제 8
특정 조건을 만족하는 유저들을 추출하여 알림 대상자 리스트를 반환하는 함수 
`get_notification_targets`를 작성하세요.

**조건:**
1. `is_active`가 `True`인 유저 중에서 선별합니다.
2. `last_login`이 7일 이상이거나, `point`가 10,000점 이상인 유저가 대상입니다.
3. 대상자의 'name'만 리스트에 담아 반환하세요.

```python
users_data = {
    "u1": {"name": "Alice", "is_active": True, "last_login": 10, "point": 5000},
    "u2": {"name": "Bob", "is_active": False, "last_login": 1, "point": 1500},
    "u3": {"name": "Charlie", "is_active": True, "last_login": 2, "point": 12000}
}


def get_notification_targets(users_data):
    result = []
    for user_data in users_data.values(): 
        
        is_active = user_data['is_active']
        last_login = user_data['last_login']
        point = user_data['point'] 
        name = user_data['name']
        
        if is_active:
            if last_login >= 7 or point >= 10000:
               result.append(user_data['name'])
    return result
```
### problem9 문제 1
여러 명의 학생 성적 데이터가 담긴 복합 리스트입니다. 
각 학생의 `grades` 리스트에서 80점 이상의 점수만 추출하여 평균을 계산하고, 
그 평균이 90점 이상인 학생의 이름만 `top_students` 리스트에 담으세요.

```python
students = [
    {"name": "John", "grades": [85, 92, 78, 100]},
    {"name": "Sarah", "grades": [88, 95, 90, 89]},
    {"name": "Tyler", "grades": [70, 75, 82, 80]},
    {"name": "Emma", "grades": [95, 98, 88, 92]}
]
top_students =[]

for student in students:
    high_grades = []  
    
    for grade in student['grades']:
        if grade >= 80:
            high_grades.append(grade)    
            
    if high_grades:
        avg = sum(high_grades) / len(high_grades)
        if avg >= 90:
            top_students.append(student['name'])
            
print(top_students)
```

### problem9 문제 3
상품 리스트를 카테고리별로 분류하여 딕셔너리를 만드세요.

```python
products = [
    {"category": "전자제품", "name": "키보드"},
    {"category": "의류", "name": "티셔츠"},
    {"category": "전자제품", "name": "마우스"},
    {"category": "전자제품", "name": "노트북"},
    {"category": "식품", "name": "사과"},
    {"category": "식품", "name": "배"},
    {"category": "의류", "name": "청바지"}
]

grouped_data = {}

for product in products:
    category = product['category']
    name = product['name']
    
    if grouped_data.get(category) == None: # 카테고리에 없으면 딕셔너리에 추가하고
        grouped_data[category] = []        # 그 카테고리에 대한 빈 리스트를 추가해라 
                                           # ex) grouped_data = { 전자제품 : []}
    grouped_data[category].append(name)    # 카테고리가 있든 없든, (없으면 위에서 만들었으니까) 해당 카테고리의 리스트에 이름을 추가해라

print(f"그룹화 결과: {grouped_data}")
  ```

## 랜덤으로 점심 조 짜기 프로젝트

```python
# 남자 12명, 여자 8명
# 한 조만 남자4 나머지는 남자2 여자2

# 남자만 있는 새로운 리스트를 하나 만들고 그 중에서 랜덤으로 4명 먼저 뽑기
# 남은 사람들 중에서 남자2, 여자2 조 만들기 

# 1조 2조 3조 4조 5조로 조 이름을 고정하고 그 조 안에 랜덤으로 뽑힌 사람을 넣는 형식 -> 딕셔너리 사용

mans = ['A', 'B', 'C', 'D',
        'E', 'F', 'G',  'H', 
        'I', 'J', 'K', 'L' ]

womans = ['a', 'b', 'c', 'd', 
         'e', 'f', 'g', 'h']

teams = {'1조' : [],
         '2조' : [],
         '3조' : [],
         '4조' : [],
         '5조' : []}

# 1. 남자 4명 조 만들기
import random
first_team = random.sample(mans, 4)
teams['1조'] = first_team


# 2. 뽑힌 남자 4명을 리스트에서 제거해서 남은 남자 구하기
remaining_mans = []

for man in mans:
    if man not in first_team:
        remaining_mans.append(man)

# 3. 남은 사람들 섞기
random.shuffle(remaining_mans)
random.shuffle(womans)

# 4. 섞은 사람들을 순서대로 2명씩 뽑아 2조~5조: 남자 2명 + 여자 2명
team_names = ['2조', '3조', '4조', '5조']

for i, team in enumerate(team_names): # 변수 i(index), 리스트 안의 값 team
    teams[team] = (                   # 딕셔너리의 value를 사람 이름이 들어있는 새 리스트로 재설정
        remaining_mans[i*2:(i+1)*2] +
        womans[i*2:(i+1)*2]
    )

# 결과 출력 -> 딕셔너리 출력
for team, members in teams.items():
    print(team, members)
  ```
